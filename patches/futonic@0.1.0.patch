diff --git a/node_modules/futonic/.bun-tag-f4064645a4fa2963 b/.bun-tag-f4064645a4fa2963
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/chunk-6w1vqkpr.js b/dist/chunk-6w1vqkpr.js
index 91fc5ce800aec999897a1dfbe21b5ec910740522..f17b89a3efd732a8b1c828ee2487dc26ca90d254 100644
--- a/dist/chunk-6w1vqkpr.js
+++ b/dist/chunk-6w1vqkpr.js
@@ -27,7 +27,8 @@ function tableToSQL(table, provider = "pg") {
     if (field.references) {
       const onDelete = field.references.onDelete ?? "restrict";
       const action = onDelete === "set-null" ? "SET NULL" : onDelete.toUpperCase();
-      columns.push(`  FOREIGN KEY (${fieldName}) REFERENCES ${field.references.model}(${field.references.field}) ON DELETE ${action}`);
+      const referencedTable = `${table.serviceId}_${field.references.model}`;
+      columns.push(`  FOREIGN KEY (${fieldName}) REFERENCES ${referencedTable}(${field.references.field}) ON DELETE ${action}`);
     }
   }
   return `CREATE TABLE IF NOT EXISTS ${table.prefixedName} (
@@ -62,6 +63,14 @@ function fieldToSQLType(field, provider) {
       else
         type = "TEXT";
       break;
+    case "binary":
+      if (provider === "pg")
+        type = "BYTEA";
+      else if (provider === "mysql")
+        type = "LONGBLOB";
+      else
+        type = "BLOB";
+      break;
     default:
       type = "TEXT";
   }
diff --git a/dist/db/internal-adapter.d.ts b/dist/db/internal-adapter.d.ts
index 74757b1a395ee7e0f5ef474686a00683eb17cb74..494e2200a3f3f4d752d5cd497d63f5b5adbfee37 100644
--- a/dist/db/internal-adapter.d.ts
+++ b/dist/db/internal-adapter.d.ts
@@ -14,8 +14,25 @@ export interface Where {
     operator?: "eq" | "ne" | "gt" | "gte" | "lt" | "lte" | "in" | "not_in";
     connector?: "AND" | "OR";
 }
+export type FilterOp = "eq" | "ne" | "gt" | "gte" | "lt" | "lte" | "in" | "not_in" | "contains" | "startsWith" | "endsWith" | "isNull" | "isNotNull";
+export type FilterNode = {
+    type: "and" | "or";
+    nodes: FilterNode[];
+} | {
+    type: "not";
+    node: FilterNode;
+} | {
+    type: "cond";
+    field: string;
+    op: FilterOp;
+    value?: unknown;
+};
 export interface FindManyOptions {
     where?: Where[];
+    /** Boolean expression tree (supports and/or/not + contains/like); combined with `where`. */
+    filter?: FilterNode;
+    /** Column projection; when omitted selects all columns. */
+    select?: string[];
     limit?: number;
     offset?: number;
     sortBy?: {
@@ -27,7 +44,10 @@ export interface TableAdapter {
     create(data: Record<string, unknown>): Promise<Record<string, unknown>>;
     findOne(where: Where[]): Promise<Record<string, unknown> | null>;
     findMany(options?: FindManyOptions): Promise<Record<string, unknown>[]>;
-    count(where?: Where[]): Promise<number>;
+    count(where?: Where[] | {
+        where?: Where[];
+        filter?: FilterNode;
+    }): Promise<number>;
     update(where: Where[], data: Record<string, unknown>): Promise<Record<string, unknown>>;
     updateMany(where: Where[], data: Record<string, unknown>): Promise<number>;
     delete(where: Where[]): Promise<void>;
diff --git a/dist/db/schema.d.ts b/dist/db/schema.d.ts
index bebee4d8ee679a23ce6f5a30812b307a6d4309cd..d69f3745bb7864d028d0159eedcf9283495ca138 100644
--- a/dist/db/schema.d.ts
+++ b/dist/db/schema.d.ts
@@ -1,6 +1,6 @@
 import type { MountedService } from "../core/service";
 export interface FieldDefinition {
-    type: "string" | "number" | "boolean" | "date" | "json";
+    type: "string" | "number" | "boolean" | "date" | "json" | "binary";
     required?: boolean;
     unique?: boolean;
     primaryKey?: boolean;
diff --git a/dist/index.js b/dist/index.js
index 27cf965a3a4d5c2adab758af5b000312096af534..78a4b563d2a1d5d6ea9da1318df148c835524324 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -99,6 +99,52 @@ function createInternalAdapter(kysely, serviceId, schema) {
     }
   });
 }
+function buildFilter(eb, node) {
+  if (!node) return eb.lit(true);
+  switch (node.type) {
+    case "and":
+      return node.nodes.length ? eb.and(node.nodes.map((n) => buildFilter(eb, n))) : eb.lit(true);
+    case "or":
+      return node.nodes.length ? eb.or(node.nodes.map((n) => buildFilter(eb, n))) : eb.lit(false);
+    case "not":
+      return eb.not(buildFilter(eb, node.node));
+    case "cond": {
+      const { field, op, value } = node;
+      switch (op) {
+        case "eq":
+          return value === null ? eb(field, "is", null) : eb(field, "=", value);
+        case "ne":
+          return value === null ? eb(field, "is not", null) : eb(field, "<>", value);
+        case "gt":
+          return eb(field, ">", value);
+        case "gte":
+          return eb(field, ">=", value);
+        case "lt":
+          return eb(field, "<", value);
+        case "lte":
+          return eb(field, "<=", value);
+        case "in":
+          return eb(field, "in", value);
+        case "not_in":
+          return eb(field, "not in", value);
+        case "contains":
+          return eb(field, "like", `%${value}%`);
+        case "startsWith":
+          return eb(field, "like", `${value}%`);
+        case "endsWith":
+          return eb(field, "like", `%${value}`);
+        case "isNull":
+          return eb(field, "is", null);
+        case "isNotNull":
+          return eb(field, "is not", null);
+        default:
+          throw new Error(`Unsupported filter op: ${op}`);
+      }
+    }
+    default:
+      throw new Error(`Unknown filter node type: ${node.type}`);
+  }
+}
 function applyWhere(query, where) {
   for (const condition of where) {
     const { field, value, operator = "eq" } = condition;
@@ -144,10 +190,13 @@ function createTableAdapter(kysely, tableName) {
       return result ?? null;
     },
     async findMany(options) {
-      let query = kysely.selectFrom(tableName).selectAll();
+      let query = options?.select && options.select.length ? kysely.selectFrom(tableName).select(options.select) : kysely.selectFrom(tableName).selectAll();
       if (options?.where) {
         query = applyWhere(query, options.where);
       }
+      if (options?.filter) {
+        query = query.where((eb) => buildFilter(eb, options.filter));
+      }
       if (options?.sortBy) {
         query = query.orderBy(options.sortBy.field, options.sortBy.direction);
       }
@@ -161,8 +210,13 @@ function createTableAdapter(kysely, tableName) {
     },
     async count(where) {
       let query = kysely.selectFrom(tableName).select(kysely.fn.count("id").as("count"));
-      if (where) {
-        query = applyWhere(query, where);
+      const whereArr = Array.isArray(where) ? where : where?.where;
+      const filter = Array.isArray(where) ? void 0 : where?.filter;
+      if (whereArr) {
+        query = applyWhere(query, whereArr);
+      }
+      if (filter) {
+        query = query.where((eb) => buildFilter(eb, filter));
       }
       const res = await query.execute();
       const count = res[0]?.count;
