---
"@spindesk/core": patch
---

Migrate to futonic `0.1.1-canary.f8d7b4b`, which adds an optional `openapi` option alongside `basePath` on the service `handler` for configuring the better-call router's OpenAPI reference route.
